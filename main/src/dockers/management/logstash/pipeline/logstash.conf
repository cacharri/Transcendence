input {
  tcp {
    port => 5000
    codec => json
    type => "nodejs"
  }

  http {
    port => 8082
    codec => json
    # El tipo por defecto para este input es "http", a menos que lo especifiques aquí.
    # No es necesario especificarlo, solo saberlo para el filtro.
  }

  file {
    path => "/var/log/nginx/access.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    type => "nginx-access"
  }

  file {
    path => "/var/log/nginx/error.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    type => "nginx-error"
  }

  file {
    path => "/var/log/backend.log"
    start_position => "beginning"
    codec => json
    type => "nodejs-file"
  }
}

filter {
  # Fastify logs (tipo JSON)
  # AÑADIR "http" a la lista de tipos para que los eventos del input HTTP también sean procesados aquí.
  if [type] in ["nodejs", "nodejs-file", "http"] {
    date {
      match => ["time", "UNIX_MS"]
      target => "@timestamp"
    }

    mutate {
      rename => { "msg" => "message" }
      convert => { "level" => "integer" }
    }

    # Configuración moderna de translate
    translate {
      source => "level"
      target => "log_level_text"
      override => true
      dictionary => {
        "10" => "trace"
        "20" => "debug"
        "30" => "info"
        "40" => "warn"
        "50" => "error"
        "60" => "fatal"
      }
      fallback => "unknown"
    }
  }

  # Logs de Nginx (access)
  if [type] == "nginx-access" {
    grok {
      match => { "message" => "%{COMBINEDAPACHELOG}" }
    }
    date {
      match => ["timestamp", "dd/MMM/yyyy:HH:mm:ss Z"]
    }
  }

  # Logs de Nginx (error)
  if [type] == "nginx-error" {
    grok {
      match => {
        "message" => "%{TIMESTAMP_ISO8601:timestamp} \[%{LOGLEVEL:log_level}\] %{GREEDYDATA:message}"
      }
    }

    mutate {
      lowercase => ["log_level"]
    }

    translate {
      source => "log_level"
      target => "log_level_text"
      override => true
      dictionary => {
        "debug" => "debug"
        "info" => "info"
        "notice" => "info"
        "warn" => "warn"
        "error" => "error"
        "crit" => "fatal"
        "alert" => "fatal"
        "emerg" => "fatal"
      }
      fallback => "unknown"
    }

    date {
      match => ["timestamp", "ISO8601"]
    }
  }

  mutate {
    add_field => {
      "environment" => "production"
      "project" => "transcendence"
    }
  }
}

output {
  elasticsearch {
    hosts => ["https://elasticsearch:9200"]
    index => "transcendence-%{+YYYY.MM.dd}"
    user => "logstash_writer"
    password => "/usr/share/elasticsearch/secrets/logstash_internal_password"
    ssl => true
    ssl_certificate_verification => true
    cacert => "/usr/share/elasticsearch/config/certs/ca/ca.crt"
    ilm_enabled => false
    manage_template => true
    template => "/usr/share/logstash/pipeline/elasticsearch-template.json"
  }
  
  stdout { codec => rubydebug }
}